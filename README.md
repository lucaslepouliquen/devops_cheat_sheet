# devops_cheat_sheet

linux questions and troubleshooting "codes"
lets discuss few questions
linux:
what is netstat?
nslookup
grep stopped pods among 100 pods
lsof -f
ps -aux
df -h
telnet
kill -9
curl
Docker:
docker exit
docker zero
docker attach
docker networking
Jenkins:
most of you get asked like my build is failed but not giving clear reason, how do you troubleshoot.
build failure always end up with codes
like 0, 1, 2, 125, 126, 127, 128, 129, 130, 137, 139, 143, 255
you are not aware, please work on these how these works and all.

Route53:
401 → Authentication issue
403 → Permission/IAM issue
404 → Resource not found
429 → Rate limit exceeded
500 → Application/server crash
502 → Reverse proxy received invalid response from backend
503 → Service unavailable (app down, no healthy targets)
504 → Backend timeout
